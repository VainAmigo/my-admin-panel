import 'package:flutter/material.dart';

class VerifiedTabView extends StatelessWidget {
  const VerifiedTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('Верифицированные')]);
  }
}